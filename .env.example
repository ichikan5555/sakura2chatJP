# さくらメール IMAP設定
IMAP_HOST=monoshare.sakura.ne.jp
IMAP_PORT=993
IMAP_USER=e14suger@monoshare.jp
IMAP_PASSWORD=
# パスワード導出モード: derive（自動生成）| manual（手動設定）
PASSWORD_MODE=derive
# deriveモードの場合のシークレット（社内ルール用）
# パスワード = PREFIX + (先頭1文字 + 末尾1文字 + ".") + SUFFIX
PASSWORD_PREFIX=araki0404.
PASSWORD_SUFFIX=junpei0822.

# Chatwork - 認証コード送信用（2FA、管理者個人トークン）
CHATWORK_AUTH_TOKEN=
# Chatwork - 運用用（メール転送・ルーム一覧取得）
CHATWORK_API_TOKEN=

# Server
PORT=3001
HOST=localhost

# Polling
POLL_INTERVAL_SEC=60

# Logging
LOG_LEVEL=info
LOG_DIR=./logs

# Database
# For local development (SQLite)
DB_PATH=./data/sakura2chat.db
# For production (PostgreSQL - Supabase)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.pxqyhmjidwwhgtqwxtcu.supabase.co:5432/postgres
